<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE log4j:configuration SYSTEM "log4j.dtd">

<!-- Note that this file is refreshed by the server every 60seconds, as specified in web.xml -->

<log4j:configuration debug="true">

        <appender name="BEANMILL" class="org.apache.log4j.net.SocketAppender">
                <!-- The active file to log to -->
                <param name="port" value="4445" />
                <param name="RemoteHost" value="localhost" />
                <param name="LocationInfo" value="true" />
                <!--layout class="org.apache.log4j.PatternLayout"-->
                        <!-- The log message pattern -->
                        <!--param name="ConversionPattern" value="%5p %d{ISO8601} [%t][%x] %c - %m%n" />
                </layout-->
        </appender>

        <!-- Loggers to filter out various class paths -->

        <logger name="org.hibernate" additivity="false">
                <level value="error"/>
                <appender-ref ref="BEANMILL" />
        </logger>
        <logger name="com.mchange" additivity="false">
                <level value="error"/>
                <appender-ref ref="BEANMILL" />
        </logger>

        <!-- Debugging loggers -->

        <!-- Uncomment to enable debug on calpoly code only -->
        <!--
        <logger name="edu.calpoly">
                <level value="debug"/>
                <appender-ref ref="ROLL" />
        </logger>
        -->

        <root>
                <priority value="debug" />
                <appender-ref ref="BEANMILL" />
        </root>

</log4j:configuration>